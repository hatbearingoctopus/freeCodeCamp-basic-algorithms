// Reverse the provided string. You may need to turn the string into an array. Result must be a string.

function reverseString (str) {
  return str.split('').reverse().join('');
  
  //split string apart between characters, use reverse to reverse it and join to join back together and return it all
