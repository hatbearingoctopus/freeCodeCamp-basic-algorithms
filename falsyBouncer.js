// Remove all falsy values from an array (false, null, 0, "", NaN, undefined).

function bouncer (arr) {
  return arr.filter(boolean);
 }
 
 //Used built in filter method to search for boolean, then returned.
